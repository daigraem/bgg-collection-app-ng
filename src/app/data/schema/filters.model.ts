export interface IFilters {
  freetext: string;
  players: number[];
  playersLimit: number[];
  playtime: number[];
  playtimeLimit: number[];
  rating: number[];
  ratingLimit: number[];
  expansions: boolean;
}
